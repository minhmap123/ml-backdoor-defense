defaults:
  - data: iotid20
  - model: mlp
  - attack: badnets
  - detection: neural_cleanse
  - train: default
  - _self_

seed: 42
experiment_name: backdoor_detection_benchmark

pipeline:
  stage: attack_train
  run_id: null
  artifact_dir: null
  attack_train_artifact_dir: null
  detection_artifact_dir: null
  skip_existing: false

# W&B logging configuration
wandb:
  enabled: true
  project: backdoor_detection_benchmark
  entity: null  # Set to your W&B username or team name
  notes: null
  tags: []
  summary_profile: compact

hydra:
  run:
    dir: results/runs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: results/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
